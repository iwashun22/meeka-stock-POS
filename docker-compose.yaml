version: '3'
services:
  app:
    build: .
    ports:
      - 3000:3000
    depends_on:
      - redis
    env_file:
      - .env

  redis:
    image: redis:8.4-alpine
    ports:
      - 6379:6379
    environment:
      - REDIS_HOST=redis